# 压缩包

全部相关内容已打成：

`/home/ubuntu/retool-910b-offline.tar.gz`

Agent Store / artifacts 盘写不进几百 MB 的包，所以压缩包放在这台机器的 `$HOME`，请从云主机拷走（scp / 网盘），不要指望从本目录直接下大文件。

解压后先读归档根目录的 `README.md`，再 `source ./env.sh`。
